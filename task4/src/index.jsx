import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import Offline from './Offline';

const rootElement = document.querySelector('#root');

ReactDOM.render(<Offline />, rootElement);
